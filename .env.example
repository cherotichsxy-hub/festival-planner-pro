# 云端配置（Supabase）。复制本文件为 .env.local 并填入真实值，重启 dev server 生效。
# 两个都不填 = 纯本地模式，一切数据只存浏览器 localStorage。
# anon key 是可公开的（权限由数据库 RLS 策略控制，见 supabase/schema.sql）。
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
